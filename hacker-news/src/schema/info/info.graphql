type Query {
  info: String!
  feed: [Link!]!
}

type Mutation {
  addPost(url: String!, description: String!): Link!
  deletePost(id: ID!): Link!
}

type Link {
  id: ID!
  description: String!
  url: String!
}
